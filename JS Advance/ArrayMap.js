const finalParticipants = ['Mike', 'Eleven', 'Will', 'Lucas', 'Dustin'];
 
const announcements = finalParticipants.map(member => {
  return member + ' is one of the main protagonist in Stranger Things';
}) 
console.log(announcements);